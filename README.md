# Dubmates

Browser dubbing party game. Default locale: English (no `/en` prefix). French studio also open at `/fr`.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
